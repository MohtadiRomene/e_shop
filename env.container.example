APP_ENV=prod
APP_DEBUG=0
APP_SECRET=replace-with-a-long-random-secret
APP_VERSION=1.0.0

MYSQL_ROOT_PASSWORD=change-me-root

DB_HOST=db
DB_PORT=3306
DB_USER=app
DB_PASSWORD=change-me
DB_NAME=e_shop

DATABASE_URL=mysql://app:change-me@db:3306/e_shop?serverVersion=8.0.32&charset=utf8mb4
MAILER_DSN=null://null

TRUSTED_PROXIES=127.0.0.1,REMOTE_ADDR
TRUSTED_HOSTS=^localhost$

RUN_MIGRATIONS=0
MIGRATIONS_STRICT=0
HTTP_PORT=8080

# Optional: create (or promote) a default admin on startup
ENSURE_DEFAULT_ADMIN=1
DEFAULT_ADMIN_EMAIL=admin@kaira.com
DEFAULT_ADMIN_PASSWORD=admin123
DEFAULT_ADMIN_NOM=Administrateur
DEFAULT_ADMIN_TEL=0123456789
DEFAULT_ADMIN_ADRESSE=123 Rue Admin, Paris

# Local only: include local MySQL service
COMPOSE_PROFILES=local
